from setuptools import setup

setup(
    name='repoorgui',
    version='0.0.1',
    packages=['repoorgui'],
    install_requires=[
        "pysimplegui",
        "gitpython",
        "platformdirs"
    ],
)
